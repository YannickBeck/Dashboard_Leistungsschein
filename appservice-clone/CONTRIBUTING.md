# Contributing

## Development

1. `npm ci`
2. `npm test`
3. `npm start`

## Pull Request Checklist

- Keine Secrets im Commit
- `npm test` erfolgreich
- `npm start` startet lokal ohne Fehler
- API `/healthz` liefert `200`
- README angepasst, wenn Setup oder Deployment geaendert wurde

